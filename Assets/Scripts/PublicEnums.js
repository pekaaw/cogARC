#pragma strict

public enum ruleFunction { 
	Grid,
	Pair,
	Tower,
	HumanReadable
};

public enum subRule {
	Addition,
	CompositeNumbers,
	WholeLiner,
	AnyWord
};

public enum CubeDesignEnum {
	ColouredBox,
	BoxImage,
	Text,
	TextAndCubeColour
};